name: Docker Build and Push

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1
        with:
          platforms: arm64

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      # If you have Dockerfile in the root directory
      - name: Build Docker Image
        run: |
          docker buildx build --platform linux/arm64/v8 -t roosteradmin/rooster-client:latest .
          docker buildx imagetools inspect roosteradmin/rooster-client:latest

      # Login to Docker Hub and push the image
      - name: Login to Docker Hub and Push Image
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push Docker Image
        run: docker push roosteradmin/rooster-client:latest